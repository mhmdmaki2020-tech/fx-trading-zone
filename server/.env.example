RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=FX Trading Zone <onboarding@resend.dev>
JWT_SECRET=change-me-to-a-long-random-string

# Site-wide privacy gate — leave both unset locally. Set both on the live deployment
# to require a username/password (HTTP Basic Auth) before anyone can see the site.
SITE_USER=
SITE_PASSWORD=
